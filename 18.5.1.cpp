#include <iostream>
#include <vector>
/*
void swapvec (std::vector<int> &vRef, int *arrRef) {
    for (int i = 0; i < vRef.size(); ++i)
        std::swap(vRef[i],*(arrRef+i));
}

int main() {
    std::vector<int> vct {1,2,3,4};
    int arr[4] {5,7,9,11};

    swapvec(vct, arr);

    for (auto const &it:vct){
        std::cout << it << " ";
    }
    std::cout << std::endl;

    return 0;
}
*/