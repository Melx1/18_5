#include <iostream>
/*
int foo(int n) {
    //an = an-1 + an-2 + an-3
    if (n > 3)
        return foo(n-1) + foo(n-2) + foo(n-3);
    else
        switch (n) {
            case 3: return 4;
            case 2: return 2;
            case 1: return 1;
            default: return -1;
        }
}

int main() {
    int n;
    std::cout << "Введите число ступенек: ";
    std::cin >> n;

    std::cout << foo(n) << std::endl;
}
*/